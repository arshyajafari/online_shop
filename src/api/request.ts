// base url
const baseUrl = "https://fakestoreapi.com";

// get all products
export const getProductsRequest = `${baseUrl}/products`;

// get user by id
export const getUserByIdRequest = `${baseUrl}/users/1`;

// get categories
export const getCategoriesRequest = `${baseUrl}/products/categories`;
